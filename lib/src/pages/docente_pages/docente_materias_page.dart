import 'package:flutter/material.dart';

class DocenteMateriasPage extends StatelessWidget {
  const DocenteMateriasPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
